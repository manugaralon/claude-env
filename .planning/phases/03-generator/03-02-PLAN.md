---
phase: 03-generator
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - claude_env/generator/generator.py
  - tests/test_generator.py
  - tests/conftest.py
autonomous: true
requirements: [GEN-01, GEN-02, GEN-03, GEN-04, GEN-05, GEN-06]

must_haves:
  truths:
    - "Generator writes per-project CLAUDE.md with plan-execute-verify, context management, and lessons.md sections"
    - "Generator writes skill SKILL.md files with YAML frontmatter and verb-phrase descriptions"
    - "Generator writes agent .md files with non-empty skills: field"
    - "Generator writes valid settings.json with lint/typecheck hooks and exitCode 2"
    - "Generator writes global CLAUDE.md with sentinel-protected managed block"
    - "Re-running generator on same project preserves user edits outside sentinel block"
    - "Path traversal attempts are rejected with ValueError"
  artifacts:
    - path: "claude_env/generator/generator.py"
      provides: "Generator class with execute() method"
      exports: ["Generator"]
      min_lines: 50
    - path: "tests/test_generator.py"
      provides: "GEN-01 through GEN-06 test coverage"
      min_lines: 100
  key_links:
    - from: "claude_env/generator/generator.py"
      to: "claude_env/generator/content_catalogue.py"
      via: "enrich_artifact_context called before every render"
      pattern: "enrich_artifact_context"
    - from: "claude_env/generator/generator.py"
      to: "claude_env/generator/sentinel.py"
      via: "merge_sentinel_block for existing files with sentinel markers"
      pattern: "merge_sentinel_block"
    - from: "claude_env/generator/generator.py"
      to: "claude_env/templates/registry.py"
      via: "TemplateRegistry.render() for all template expansion"
      pattern: "self._registry.render"
---

<objective>
Implement the Generator class that executes a GenerationPlan by writing all artifacts to disk, and the comprehensive test suite covering GEN-01 through GEN-06.

Purpose: This is the core I/O layer that turns data (GenerationPlan) into files on disk. It must handle per-project files, global files, sentinel merge on re-run, and path traversal protection.
Output: Generator class + 15+ tests covering all 6 requirements.
</objective>

<execution_context>
@/home/manuel/.claude/get-shit-done/workflows/execute-plan.md
@/home/manuel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-generator/03-RESEARCH.md
@.planning/phases/03-generator/03-01-SUMMARY.md

@claude_env/models/generation_plan.py
@claude_env/models/domain_profile.py
@claude_env/pipeline/environment_planner.py
@claude_env/templates/registry.py
@claude_env/generator/sentinel.py
@claude_env/generator/content_catalogue.py
@templates/claude_md_project.j2
@templates/skill_stub.j2
@templates/agent_stub.j2
@templates/settings_json.j2
@claude_env/profiles/web.yaml
@tests/conftest.py

<interfaces>
<!-- From Plan 01 outputs (sentinel.py, content_catalogue.py) -->

From claude_env/generator/sentinel.py:
```python
SENTINEL_HEADER: str  # "# BEGIN CLAUDE-ENV MANAGED — do not edit this block manually\n"
SENTINEL_FOOTER: str  # "# END CLAUDE-ENV MANAGED\n"

def wrap_with_sentinel(content: str) -> str: ...
def merge_sentinel_block(existing: str, new_managed_content: str) -> str: ...
def has_sentinel(content: str) -> bool: ...
```

From claude_env/generator/content_catalogue.py:
```python
def enrich_artifact_context(artifact: Artifact, plan: GenerationPlan) -> dict[str, object]: ...
```

From claude_env/models/generation_plan.py:
```python
class OutputLayer(StrEnum):
    GLOBAL = "global"
    PROJECT = "project"

class Artifact(BaseModel):
    target_path: str
    template_id: str
    context: dict[str, object]
    layer: OutputLayer

class GenerationPlan(BaseModel):
    project_name: str
    domain: str
    artifacts: list[Artifact]
```

From claude_env/templates/registry.py:
```python
class TemplateRegistry:
    def __init__(self, templates_dir: Path) -> None: ...
    def render(self, template_id: str, context: dict[str, object]) -> str: ...
```

From claude_env/pipeline/environment_planner.py:
```python
def plan(spec: ProjectSpec, profile: DomainProfile, available_templates: list[str]) -> GenerationPlan: ...
```
Note: planner produces only PROJECT-layer artifacts. Generator must handle GLOBAL layer and settings.json injection.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generator class implementation</name>
  <files>claude_env/generator/generator.py</files>
  <read_first>
    - claude_env/generator/sentinel.py (merge_sentinel_block, has_sentinel, wrap_with_sentinel signatures)
    - claude_env/generator/content_catalogue.py (enrich_artifact_context signature)
    - claude_env/models/generation_plan.py (Artifact, GenerationPlan, OutputLayer)
    - claude_env/templates/registry.py (TemplateRegistry.render signature)
    - claude_env/pipeline/environment_planner.py (how plan() builds artifacts — to understand context shape)
    - templates/claude_md_project.j2 (verify template exists and expected vars)
  </read_first>
  <behavior>
    - Generator(registry).execute(plan, project_root, global_root) writes all artifacts to disk and returns list of written paths
    - PROJECT-layer artifacts are written under project_root/.claude/
    - GLOBAL-layer artifacts are written under global_root/
    - Parent directories are created automatically (mkdir parents=True, exist_ok=True)
    - If target file exists and has sentinel markers, merge_sentinel_block is used instead of overwrite
    - If target file exists without sentinel markers, file is overwritten (new generation replaces old)
    - Path traversal (target_path containing ..) raises ValueError
    - All content written with encoding="utf-8"
  </behavior>
  <action>
    Create `claude_env/generator/generator.py` with:

    ```python
    from __future__ import annotations

    from pathlib import Path

    from claude_env.generator.content_catalogue import enrich_artifact_context
    from claude_env.generator.sentinel import has_sentinel, merge_sentinel_block
    from claude_env.models.generation_plan import Artifact, GenerationPlan, OutputLayer
    from claude_env.templates.registry import TemplateRegistry
    ```

    Class `Generator`:
    - `__init__(self, registry: TemplateRegistry) -> None`: store `self._registry = registry`
    - `execute(self, plan: GenerationPlan, project_root: Path, global_root: Path) -> list[Path]`:
      1. Initialize `written: list[Path] = []`
      2. For each artifact in `plan.artifacts`:
         a. Call `self._resolve_path(artifact, project_root, global_root)` to get absolute target path
         b. Call `enrich_artifact_context(artifact, plan)` to get full context dict
         c. Call `self._registry.render(artifact.template_id, context)` to get rendered content
         d. If `target.exists()` and `has_sentinel(target.read_text(encoding="utf-8"))`:
            - Read existing content, call `merge_sentinel_block(existing, content)` to get merged
            - Use merged as content
         e. Call `target.parent.mkdir(parents=True, exist_ok=True)`
         f. Call `target.write_text(content, encoding="utf-8")`
         g. Append target to written
      3. Return written

    - `_resolve_path(self, artifact: Artifact, project_root: Path, global_root: Path) -> Path`:
      1. Determine layer_root: `project_root / ".claude"` if `artifact.layer == OutputLayer.PROJECT`, else `global_root`
      2. Compute `target = (layer_root / artifact.target_path).resolve()`
      3. Security check: `if not str(target).startswith(str(layer_root.resolve())):`  raise `ValueError(f"Path traversal detected: {artifact.target_path}")`
      4. Return target

    Docstrings on class and both methods. Use `from __future__ import annotations` at top.
  </action>
  <verify>
    <automated>uv run ruff check claude_env/generator/generator.py && uv run mypy claude_env/generator/generator.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class Generator" claude_env/generator/generator.py` returns 1
    - `grep -c "def execute" claude_env/generator/generator.py` returns 1
    - `grep -c "def _resolve_path" claude_env/generator/generator.py` returns 1
    - `grep "enrich_artifact_context" claude_env/generator/generator.py` matches
    - `grep "merge_sentinel_block" claude_env/generator/generator.py` matches
    - `grep "has_sentinel" claude_env/generator/generator.py` matches
    - `grep "Path traversal detected" claude_env/generator/generator.py` matches
    - `grep 'encoding="utf-8"' claude_env/generator/generator.py` matches
    - `grep "parents=True, exist_ok=True" claude_env/generator/generator.py` matches
    - `uv run ruff check claude_env/generator/generator.py` passes
    - `uv run mypy claude_env/generator/generator.py` passes
  </acceptance_criteria>
  <done>Generator class exists with execute() and _resolve_path(), uses sentinel merge for existing files, enriches context via catalogue, rejects path traversal</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Comprehensive test suite for GEN-01 through GEN-06</name>
  <files>tests/test_generator.py, tests/conftest.py</files>
  <read_first>
    - claude_env/generator/generator.py (just created — understand execute/resolve API)
    - claude_env/generator/sentinel.py (wrap_with_sentinel for test setup)
    - claude_env/generator/content_catalogue.py (understand enrichment for assertion values)
    - claude_env/models/generation_plan.py (Artifact, GenerationPlan, OutputLayer constructors)
    - claude_env/pipeline/environment_planner.py (understand how plan() builds artifacts — use plan() in tests)
    - claude_env/profiles/web.yaml (skill_slugs, agent_slugs, claude_md_sections for web profile fixture)
    - templates/claude_md_project.j2 (to know what output to assert)
    - templates/skill_stub.j2 (to know what skill output looks like)
    - templates/agent_stub.j2 (to know what agent output looks like)
    - templates/settings_json.j2 (to know what settings output looks like)
    - tests/conftest.py (existing fixtures to extend)
  </read_first>
  <behavior>
    - GEN-01: Generated CLAUDE.md is <=200 lines, contains "Plan", "Execute", "Verify", "Context", "lessons.md"
    - GEN-02: Generated skills/ has 4 SKILL.md files (web profile), each with YAML frontmatter containing "name:" and "description:", description is verb-phrase
    - GEN-03: Generated agents/ has 2 agent files (web profile), each with "skills:" in frontmatter containing non-empty list
    - GEN-04: Generated settings.json is valid JSON, has hooks.PostToolUse with exitCode: 2
    - GEN-05: Generator can write to global_root/CLAUDE.md, preserving existing user content outside sentinel block
    - GEN-06: Running generator twice produces identical output (idempotency); user edits outside sentinel block survive re-run
    - Path traversal artifact raises ValueError
  </behavior>
  <action>
    1. Update `tests/conftest.py` to add a shared fixture:
       - `@pytest.fixture` `real_registry() -> TemplateRegistry`: returns `TemplateRegistry(Path(__file__).parent.parent / "templates")` using resolved absolute path. This fixture uses the real templates directory.
       - `@pytest.fixture` `web_plan(real_registry: TemplateRegistry) -> GenerationPlan`: builds a GenerationPlan using `plan()` from `claude_env.pipeline.environment_planner` with a web ProjectSpec and web DomainProfile loaded from `claude_env/profiles/web.yaml`. Uses `real_registry.list_templates()` for available_templates. This gives a realistic plan with 1 CLAUDE.md + 4 skills + 2 agents.

    2. Create `tests/test_generator.py` with these test functions. All use `tmp_path` for filesystem isolation. Import `Generator` from `claude_env.generator.generator`, `Artifact`/`GenerationPlan`/`OutputLayer` from models, `wrap_with_sentinel` from sentinel.

       **GEN-01 tests:**
       - `test_claude_md_line_count(tmp_path, real_registry, web_plan)`: execute web_plan, read `.claude/CLAUDE.md`, assert `len(content.splitlines()) <= 200`
       - `test_claude_md_required_sections(tmp_path, real_registry, web_plan)`: execute, assert rendered CLAUDE.md contains "Plan" (case-insensitive search ok), "Execute", "Verify", "Context", "lessons.md"

       **GEN-02 tests:**
       - `test_skill_files_created(tmp_path, real_registry, web_plan)`: execute, assert `(tmp_path / ".claude/skills/fix-issue/SKILL.md").exists()` and same for create-pr, run-lint, update-deps (4 total from web profile)
       - `test_skill_frontmatter(tmp_path, real_registry, web_plan)`: execute, read any skill SKILL.md, assert starts with `---`, contains `name:`, contains `description:`, description value starts with a verb (action word)

       **GEN-03 tests:**
       - `test_agent_files_created(tmp_path, real_registry, web_plan)`: execute, assert `(tmp_path / ".claude/agents/security-reviewer.md").exists()` and accessibility-auditor.md
       - `test_agent_skills_field(tmp_path, real_registry, web_plan)`: execute, read agent file, assert contains `skills:` followed by at least one `  - ` entry (non-empty skills list)

       **GEN-04 tests:**
       - `test_settings_json(tmp_path, real_registry)`: create a minimal plan with a single settings.json Artifact(target_path="settings.json", template_id="settings_json.j2", context={"hooks": [{"command": "/usr/bin/ruff check .", "exit_code": 2}]}, layer=PROJECT). Execute, read `.claude/settings.json`, `json.loads()` succeeds, assert `data["hooks"]["PostToolUse"][0]["hooks"][0]["exitCode"] == 2`, assert `data["hooks"]["PostToolUse"][0]["hooks"][0]["command"] == "/usr/bin/ruff check ."`

       **GEN-05 tests:**
       - `test_global_claude_md(tmp_path, real_registry)`: create plan with GLOBAL layer Artifact for CLAUDE.md. Pre-create `global_root/CLAUDE.md` with "# My global config\n". Execute with global_root=tmp_path/"global". Assert file exists, contains "My global config" (user content preserved), contains sentinel markers.

       **GEN-06 tests:**
       - `test_idempotent_rerun(tmp_path, real_registry, web_plan)`: execute twice, compare all generated files' contents, assert identical
       - `test_user_edits_preserved_on_rerun(tmp_path, real_registry)`: create plan with GLOBAL Artifact. Execute once. Read generated file, append "# USER EDIT" after the sentinel block, write back. Execute again. Assert "# USER EDIT" still present in file.

       **Security tests:**
       - `test_path_traversal_rejected(tmp_path, real_registry)`: create plan with Artifact(target_path="../../etc/evil", ...). Execute, assert `pytest.raises(ValueError, match="Path traversal")`.

       **Basic tests:**
       - `test_execute_returns_written_paths(tmp_path, real_registry, web_plan)`: execute, assert return value is list of Path, length equals number of artifacts
       - `test_creates_parent_dirs(tmp_path, real_registry, web_plan)`: execute, assert `.claude/skills/fix-issue/` directory exists (3 levels deep)
  </action>
  <verify>
    <automated>uv run pytest tests/test_generator.py -x -v && uv run pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" tests/test_generator.py` returns 12 or more
    - `grep "test_claude_md_line_count" tests/test_generator.py` matches
    - `grep "test_claude_md_required_sections" tests/test_generator.py` matches
    - `grep "test_skill_files_created" tests/test_generator.py` matches
    - `grep "test_skill_frontmatter" tests/test_generator.py` matches
    - `grep "test_agent_files_created" tests/test_generator.py` matches
    - `grep "test_agent_skills_field" tests/test_generator.py` matches
    - `grep "test_settings_json" tests/test_generator.py` matches
    - `grep "test_global_claude_md" tests/test_generator.py` matches
    - `grep "test_idempotent_rerun" tests/test_generator.py` matches
    - `grep "test_user_edits_preserved" tests/test_generator.py` matches
    - `grep "test_path_traversal_rejected" tests/test_generator.py` matches
    - `uv run pytest tests/test_generator.py -x -q` passes with 12+ tests
    - `uv run pytest tests/ -x -q` passes (no regressions)
    - `uv run ruff check tests/test_generator.py` passes
  </acceptance_criteria>
  <done>12+ tests pass covering GEN-01 (line count, required sections), GEN-02 (skill files, frontmatter), GEN-03 (agent files, skills field), GEN-04 (settings.json validity), GEN-05 (global CLAUDE.md), GEN-06 (idempotency, user edit preservation), and path traversal rejection; full test suite green</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_generator.py -v` — all 12+ generator tests green
- `uv run pytest tests/ -v` — full suite green (no regressions from Phase 1/2 tests)
- `uv run ruff check claude_env/ tests/` — no lint errors anywhere
- `uv run mypy claude_env/` — no type errors
- Verify GEN-01: `python -c "print(len(open('.claude/CLAUDE.md').readlines()))"` shows <=200 (when run against tmp_path output)
- Verify GEN-04: `python -c "import json; json.load(open('.claude/settings.json'))"` succeeds
- Verify GEN-06: two consecutive runs produce byte-identical output
</verification>

<success_criteria>
- Generator.execute() writes all artifact types: CLAUDE.md, skills, agents, settings.json
- Generator handles both PROJECT and GLOBAL layers correctly
- Sentinel merge preserves user edits on re-run
- Path traversal is caught and rejected
- 12+ tests pass covering all 6 GEN requirements
- Full test suite (all phases) passes with no regressions
- ruff and mypy clean on all code
</success_criteria>

<output>
After completion, create `.planning/phases/03-generator/03-02-SUMMARY.md`
</output>
