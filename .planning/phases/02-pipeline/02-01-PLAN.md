---
phase: 02-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - claude_env/models/project_spec.py
  - claude_env/models/generation_plan.py
  - claude_env/pipeline/__init__.py
  - pyproject.toml
  - tests/fixtures/specs/simple_web.yaml
  - tests/fixtures/specs/cli_tool.yaml
  - tests/fixtures/specs/simple_web.md
autonomous: true
requirements: [PIPE-01, PIPE-02, PIPE-03, PIPE-04]

must_haves:
  truths:
    - "ProjectSpec model validates freeform-compatible fields with extra='forbid'"
    - "GenerationPlan model holds Artifact list with layer + template_id + context"
    - "anthropic SDK is installable and importable"
    - "Test fixtures exist for structured spec parsing tests"
  artifacts:
    - path: "claude_env/models/project_spec.py"
      provides: "ProjectSpec Pydantic v2 model"
      exports: ["ProjectSpec"]
      contains: "extra=\"forbid\""
    - path: "claude_env/models/generation_plan.py"
      provides: "GenerationPlan, Artifact, OutputLayer"
      exports: ["GenerationPlan", "Artifact", "OutputLayer"]
      contains: "extra=\"forbid\""
    - path: "claude_env/pipeline/__init__.py"
      provides: "Pipeline package init"
    - path: "tests/fixtures/specs/simple_web.yaml"
      provides: "YAML spec fixture for PIPE-02 tests"
    - path: "tests/fixtures/specs/simple_web.md"
      provides: "Markdown spec fixture for PIPE-02 tests"
  key_links:
    - from: "claude_env/models/project_spec.py"
      to: "claude_env/models/generation_plan.py"
      via: "project_name field mirrors ProjectSpec.name"
      pattern: "project_name: str"
---

<objective>
Define the data contracts (ProjectSpec, GenerationPlan) that every pipeline component consumes or produces, add the anthropic SDK dependency, and create test fixture files.

Purpose: All three pipeline components (InputNormalizer, DomainClassifier, EnvironmentPlanner) depend on these models. Defining contracts first prevents the "scavenger hunt" anti-pattern.
Output: Two model files, pipeline package init, anthropic in pyproject.toml, spec fixture files.
</objective>

<execution_context>
@/home/manuel/.claude/get-shit-done/workflows/execute-plan.md
@/home/manuel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pipeline/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts from Phase 1 that these models must align with. -->

From claude_env/models/domain_profile.py:
```python
class DomainProfile(BaseModel):
    model_config = ConfigDict(extra="forbid")
    domain: str
    display_name: str
    description: str
    skill_slugs: list[str]
    agent_slugs: list[str]
    claude_md_sections: list[str]
    hook_templates: list[str]
    detection_signals: list[str]

def load_profile(path: Path) -> DomainProfile:
```

From claude_env/templates/registry.py:
```python
class TemplateRegistry:
    def render(self, template_id: str, context: dict[str, object]) -> str: ...
    def list_templates(self) -> list[str]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define ProjectSpec and GenerationPlan Pydantic models</name>
  <files>claude_env/models/project_spec.py, claude_env/models/generation_plan.py, tests/test_models.py</files>
  <read_first>
    claude_env/models/domain_profile.py
    claude_env/models/__init__.py
    pyproject.toml
    .planning/phases/02-pipeline/02-RESEARCH.md
  </read_first>
  <behavior>
    - ProjectSpec(name="x", description="y") creates valid instance with defaults: domain_hint="general", languages=[], tech_stack=[], constraints=[], known_skills=[]
    - ProjectSpec with extra field raises ValidationError (extra="forbid")
    - ProjectSpec(name="x", description="y", domain_hint="web", languages=["python"], tech_stack=["react"]) round-trips through model_validate(model.model_dump())
    - Artifact with all fields creates valid instance
    - OutputLayer.GLOBAL == "global" and OutputLayer.PROJECT == "project"
    - GenerationPlan(project_name="x", domain="web", artifacts=[]) is valid with empty list
    - GenerationPlan with extra field raises ValidationError
  </behavior>
  <action>
Create `claude_env/models/project_spec.py`:

```python
from __future__ import annotations
from pydantic import BaseModel, ConfigDict, Field

class ProjectSpec(BaseModel):
    model_config = ConfigDict(extra="forbid")

    name: str
    description: str
    domain_hint: str = "general"
    languages: list[str] = Field(default_factory=list)
    tech_stack: list[str] = Field(default_factory=list)
    constraints: list[str] = Field(default_factory=list)
    known_skills: list[str] = Field(default_factory=list)
```

Create `claude_env/models/generation_plan.py`:

```python
from __future__ import annotations
from enum import Enum
from pydantic import BaseModel, ConfigDict

class OutputLayer(str, Enum):
    GLOBAL = "global"
    PROJECT = "project"

class Artifact(BaseModel):
    model_config = ConfigDict(extra="forbid")

    target_path: str
    template_id: str
    context: dict[str, object]
    layer: OutputLayer

class GenerationPlan(BaseModel):
    model_config = ConfigDict(extra="forbid")

    project_name: str
    domain: str
    artifacts: list[Artifact]
```

Create `tests/test_models.py` with tests for all behaviors listed above. Write tests FIRST (RED), then the model files (GREEN).
  </action>
  <verify>
    <automated>uv run pytest tests/test_models.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - claude_env/models/project_spec.py contains `class ProjectSpec(BaseModel):`
    - claude_env/models/project_spec.py contains `model_config = ConfigDict(extra="forbid")`
    - claude_env/models/project_spec.py contains `domain_hint: str = "general"`
    - claude_env/models/project_spec.py contains `tech_stack: list[str] = Field(default_factory=list)`
    - claude_env/models/generation_plan.py contains `class OutputLayer(str, Enum):`
    - claude_env/models/generation_plan.py contains `class Artifact(BaseModel):`
    - claude_env/models/generation_plan.py contains `class GenerationPlan(BaseModel):`
    - claude_env/models/generation_plan.py contains `model_config = ConfigDict(extra="forbid")`
    - tests/test_models.py contains `def test_project_spec_defaults`
    - tests/test_models.py contains `def test_project_spec_extra_field_rejected`
    - tests/test_models.py contains `def test_generation_plan_extra_field_rejected`
    - `uv run pytest tests/test_models.py -x -q` exits 0
    - `uv run mypy claude_env/models/project_spec.py claude_env/models/generation_plan.py` exits 0
  </acceptance_criteria>
  <done>Both Pydantic models exist with extra="forbid", all fields typed, defaults set, and all tests pass including mypy</done>
</task>

<task type="auto">
  <name>Task 2: Add anthropic dependency, create pipeline package, and spec fixtures</name>
  <files>pyproject.toml, claude_env/pipeline/__init__.py, tests/fixtures/specs/simple_web.yaml, tests/fixtures/specs/cli_tool.yaml, tests/fixtures/specs/simple_web.md</files>
  <read_first>
    pyproject.toml
    claude_env/profiles/web.yaml
    claude_env/profiles/cli.yaml
  </read_first>
  <action>
1. Add `anthropic>=0.96` to dependencies in `pyproject.toml` (append to the existing dependencies list after "rich>=14.0").

2. Run `uv sync` to install.

3. Create `claude_env/pipeline/__init__.py` as empty file.

4. Create `tests/fixtures/specs/` directory.

5. Create `tests/fixtures/specs/simple_web.yaml`:
```yaml
name: habit-tracker
description: A web application for tracking daily habits with streaks and reminders
domain_hint: web
languages:
  - python
  - typescript
tech_stack:
  - react
  - fastapi
  - postgres
constraints:
  - offline-first
known_skills: []
```

6. Create `tests/fixtures/specs/cli_tool.yaml`:
```yaml
name: file-organizer
description: CLI tool that organizes files by type and date into structured directories
domain_hint: cli
languages:
  - python
tech_stack:
  - typer
  - rich
constraints: []
known_skills: []
```

7. Create `tests/fixtures/specs/simple_web.md`:
```markdown
# habit-tracker

## Description

A web application for tracking daily habits with streaks and reminders.

## Stack

- react
- fastapi
- postgres
```
  </action>
  <verify>
    <automated>uv sync && python -c "import anthropic; print(anthropic.__version__)" && python -c "import claude_env.pipeline" && test -f tests/fixtures/specs/simple_web.yaml && test -f tests/fixtures/specs/cli_tool.yaml && test -f tests/fixtures/specs/simple_web.md && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains `"anthropic>=0.96"`
    - claude_env/pipeline/__init__.py exists (empty file)
    - tests/fixtures/specs/simple_web.yaml contains `name: habit-tracker`
    - tests/fixtures/specs/simple_web.yaml contains `domain_hint: web`
    - tests/fixtures/specs/cli_tool.yaml contains `name: file-organizer`
    - tests/fixtures/specs/cli_tool.yaml contains `domain_hint: cli`
    - tests/fixtures/specs/simple_web.md contains `# habit-tracker`
    - tests/fixtures/specs/simple_web.md contains `## Stack`
    - `python -c "import anthropic"` exits 0
    - `python -c "import claude_env.pipeline"` exits 0
  </acceptance_criteria>
  <done>anthropic SDK installed, pipeline package importable, all 3 fixture files exist with correct content</done>
</task>

</tasks>

<verification>
uv run pytest tests/test_models.py -x -q && uv run mypy claude_env/models/ && uv run ruff check claude_env/models/ claude_env/pipeline/
</verification>

<success_criteria>
- ProjectSpec and GenerationPlan models importable and validated
- anthropic SDK importable
- Pipeline package exists
- Test fixtures ready for Plan 02 and 03
- ruff + mypy clean on new files
</success_criteria>

<output>
After completion, create `.planning/phases/02-pipeline/02-01-SUMMARY.md`
</output>
